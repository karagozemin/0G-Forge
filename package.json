{
  "name": "og-terminal-builder",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm --filter @og/cli dev",
    "build": "pnpm -r --filter ./apps/* --filter ./packages/* run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  }
}