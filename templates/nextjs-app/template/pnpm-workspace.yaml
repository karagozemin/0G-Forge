# Allow native dependency install scripts so `pnpm install` works out of the box.
# `allowBuilds` is for pnpm >= 11, `onlyBuiltDependencies` for pnpm 10.
allowBuilds:
  sharp: true
  "@swc/core": true
  esbuild: true
onlyBuiltDependencies:
  - sharp
  - "@swc/core"
  - esbuild
